projects:
  app:
    schemPath: src/schema.graphql
    extensions:
      endpoints:
        default: http://localhost:3000
  prisma:
    schemaPath: src/generated/prisma-client/prisma-schema.js
    extensions:
      prisma: prisma/prisma.yml